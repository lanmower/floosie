const mk = (base, meta) => (meta !== undefined ? { ...base, meta } : base);
export const json = (data, meta) => mk({ type: "json", data }, meta);
export const binary = (data, meta) => mk({ type: "binary", data }, meta);
export const text = (data, meta) => mk({ type: "text", data }, meta);
export const raw = (data, meta) => mk({ type: "raw", data }, meta);
export const image = (data, meta) => mk({ type: "image", data }, meta);
export const video = (data, meta) => mk({ type: "video", data }, meta);
export const ndjson = (data, meta) => mk({ type: "ndjson", data }, meta);
export const csv = (data, meta) => mk({ type: "csv", data }, meta);
export const xml = (data, meta) => mk({ type: "xml", data }, meta);
export const yaml = (data, meta) => mk({ type: "yaml", data }, meta);
export const markdown = (data, meta) => mk({ type: "markdown", data }, meta);
export const html = (data, meta) => mk({ type: "html", data }, meta);
export const sql = (data, meta) => mk({ type: "sql", data }, meta);
export const httpRequest = (data, meta) => mk({ type: "http-request", data }, meta);
export const httpResponse = (data, meta) => mk({ type: "http-response", data }, meta);
export const websocket = (data, meta) => mk({ type: "websocket", data }, meta);
export const sse = (data, meta) => mk({ type: "sse", data }, meta);
export const audio = (data, meta) => mk({ type: "audio", data }, meta);
export const pdf = (data, meta) => mk({ type: "pdf", data }, meta);
export const archive = (data, meta) => mk({ type: "archive", data }, meta);
export const embedding = (data, meta) => mk({ type: "embedding", data }, meta);
export const token = (data, meta) => mk({ type: "token", data }, meta);
export const delta = (data, meta) => mk({ type: "delta", data }, meta);
export const uint8 = (data, meta) => mk({ type: "uint8", data }, meta);
export const int32 = (data, meta) => mk({ type: "int32", data }, meta);
export const float64 = (data, meta) => mk({ type: "float64", data }, meta);
export const bool = (data, meta) => mk({ type: "bool", data }, meta);
export const timestamp = (data, meta) => mk({ type: "timestamp", data }, meta);
export const uuid = (data, meta) => mk({ type: "uuid", data }, meta);
export const error = (data, meta) => mk({ type: "error", data }, meta);
export const signal = (data, meta) => mk({ type: "signal", data }, meta);
export const nil = (meta) => mk({ type: "null", data: null }, meta);
export const protobuf = (data, meta) => mk({ type: "protobuf", data }, meta);
export const msgpack = (data, meta) => mk({ type: "msgpack", data }, meta);
export const cbor = (data, meta) => mk({ type: "cbor", data }, meta);
export const arrow = (data, meta) => mk({ type: "arrow", data }, meta);
export const parquet = (data, meta) => mk({ type: "parquet", data }, meta);
export const geojson = (data, meta) => mk({ type: "geojson", data }, meta);
export const jwt = (data, meta) => mk({ type: "jwt", data }, meta);
export const graphql = (data, meta) => mk({ type: "graphql", data }, meta);
export const rpc = (data, meta) => mk({ type: "rpc", data }, meta);
export const event = (data, meta) => mk({ type: "event", data }, meta);
export const span = (data, meta) => mk({ type: "span", data }, meta);
export const metric = (data, meta) => mk({ type: "metric", data }, meta);
export const log = (data, meta) => mk({ type: "log", data }, meta);
export const command = (data, meta) => mk({ type: "command", data }, meta);
export const frame = (data, meta) => mk({ type: "frame", data }, meta);
export const patch = (data, meta) => mk({ type: "patch", data }, meta);
export const multipart = (data, meta) => mk({ type: "multipart", data }, meta);
//# sourceMappingURL=chunk-factories.js.map